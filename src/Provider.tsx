import React from 'react';
import {StyleSheet, View} from 'react-native';

type ProviderPropsType = {};

export const Provider = ({}: ProviderPropsType) => {
  return <View style={styles.container} />;
};

const styles = StyleSheet.create({
  container: {},
});
